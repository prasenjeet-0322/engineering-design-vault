# KPI 15 — Modern CSS

## Overview
*Status: Ready for KPI 15 Study Material & Evaluation.*

## Learning Objectives
- Native CSS Nesting, `:has()`, `:is()`, `:where()`, Cascade Layers (`@layer`), CSS scoping (`@scope`).
